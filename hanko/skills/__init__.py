"""Research skills this agent contributes back to the RYO tool surface.

Each is read-only, follows the published tool contract, and honours the
honesty convention: a missing input produces a null, never a default.
"""
